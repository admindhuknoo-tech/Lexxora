# licensing/ — quick start

See `../ARCHITECTURE_V7.md` at the repo root for the full plan (version tree,
both flows end-to-end, checklists). This folder is the code; that file is
the map.

- `core/`        shared crypto + types, used by both tracks
- `desktop/`     V7 — offline, single-device activation keys
- `web/`         V7.1 — time-based subscriptions (DAY/WEEK/MONTH, IDR)
- `admin-tools/` scripts you run yourself, offline — never shipped/deployed
